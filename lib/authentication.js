var Joi = require('joi')
var User = require('../models/user').User

exports.register = {
	handler: function(request, reply) {
		reply('In development')
	}
}

exports.login = {
	handler: function(request, reply) {
		reply('In development')
	}
}

exports.logout = {
	handler: function(request, reply) {
		reply('In Development')
	}
}